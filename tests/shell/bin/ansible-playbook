#!/usr/bin/env bash
# Fake ansible-playbook for shell tests; logs argv when FAKE_ANSIBLE_LOG is set.
set -euo pipefail

if [[ -n "${FAKE_ANSIBLE_LOG:-}" ]]; then
  printf '%q ' "$@" >>"$FAKE_ANSIBLE_LOG"
  echo >>"$FAKE_ANSIBLE_LOG"
fi

exit 0
