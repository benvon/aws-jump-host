---
- name: Configure jump hosts
  hosts: jump_hosts
  gather_facts: true
  become: true
  connection: aws_ssm
  vars:
    ansible_aws_ssm_region: "{{ lookup('env', 'AWS_REGION') | default('us-east-1', true) }}"

  roles:
    - role: home_volume
    - role: base_hardening
    - role: user_accounts
