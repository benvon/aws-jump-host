---
- name: Decommission jump host user access
  hosts: jump_hosts
  gather_facts: true
  become: true
  connection: aws_ssm
  vars:
    users: []
    ansible_aws_ssm_region: "{{ lookup('env', 'AWS_REGION') | default('us-east-1', true) }}"

  roles:
    - role: user_accounts
